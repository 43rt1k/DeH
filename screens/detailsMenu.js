import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';


const DetailsMenu = ({ navigation }) => {
    return (
        <View>
            <Text>
                Choose a new location for weather prediction
            </Text>

            
        </View>
    );
};

const styles = StyleSheet.create({
 
});

export default DetailsMenu;
